{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "api-server/Dockerfile"
  },
  "deploy": {
    "startCommand": "npm --prefix api-server start",
    "restartPolicyType": "ALWAYS",
    "restartPolicyMaxRetries": 10
  }
}

